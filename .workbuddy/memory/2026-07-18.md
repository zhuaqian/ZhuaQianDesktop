# 2026-07-18

## 编程动手能力闭环（Epic D 收敛，P0）

用户给出对标 Codex/Claude Code/Trae 的差距分析，要求先把"编程动手能力"做成可演示闭环。
盘点现有代码：AgentPipeline / CodingAgentSession / WorkspaceScanSummary / CommandRunRecorder /
AgentPlanRunner 已存在（约 70% 骨架），但都是一次性/报告型——缺迭代自愈闭环、补丁内核、通用扫描、git 工作流。

- 写并**实跑**了可演示闭环参考实现 `outputs/coding-loop/coding_loop.py`（纯 Python 标准库）：
  在 `sample_repo/` 注入 bug → 扫描 → 发现 test → 跑测试失败 → 解析错误 → 打补丁 → 重跑通过 → 出 diff。
  已验证（iter1 FAIL exit1 → iter2 PASS exit0，改了 calc.py）。附 README.md。
- 把同一算法移植进 ZhuaQian C#（贴合现有 AgentPipeline/CommandResult/AgentCommand 类型）：
  - 新增 `src/Agent/PatchExecutor.cs`（缺的"代码编辑内核"：add/edit/delete + 统一 diff + 回滚 manifest，过 PermissionGate）
  - 新增 `src/Agent/FixLoopRunner.cs`（迭代自愈闭环 Analyze→Plan→ApplyPatch→RunTests→Fix→Review→Done；含 IFixStrategy 接口，生产由 LLM 填充；ParseErrors 支持 csc 'file(line,col): error' 与 unittest 'FAIL:'）
  - 新增 `src/Agent/GitWorkflowExecutor.cs`（缺的 git 工作流：diff/status/branch/commit/export-patch）
  - `src/Agent/AgentPipelineFactory.cs` 注册 PatchExecutor + GitWorkflowExecutor（root 取 configDir 父目录）
- 四项 C# 文件经跳过字符串/注释的括号配平检查，均 PARENS BALANCED。
- **约束重申**：沙箱禁 csc，上述 C# 无法在此编译，需用户本地 `build.ps1` 验证（算法已用 Python 原型端到端验证）。
- 待补：`WorkspaceScanSummary` 仍硬编码本项目（写死 3895 预算/ZhuaQianDesktop.cs），需重构成通用扫描器；FixStrategy 生产实现（接 LLM）未写。

### 我的独立实现（Agent.Coding 子命名空间，完整测试）

与上述并行进程的 PatchExecutor/FixLoopRunner/GitWorkflowExecutor（Agent 命名空间、接 AgentPipeline 的 ICommandExecutor）平行。
我在 `src/Agent/Coding/`（namespace `ZhuaQianDesktopApp.Agent.Coding`）写了 6 个纯库模块 + 6 个测试，定位不同——纯库被顶层编排组合，不塞进 Executor 字典，内部各自走权限门：

- `ProjectAnalyzer`：通用项目扫描器（语言/框架/构建/测试/入口/包文件），不写死本项目，支持 C#/JS/TS/Python/Go/Rust/Java
- `CodePatcher`：可控 patch（Create/Modify/Delete + unified diff + .bak 回滚 + dry-run + 路径防逃逸 + OldContent 前置条件校验），走 permFileWrite
- `ErrorParser`：MSBuild/csc/PowerShell/Python/Go/Rust/Jest 错误解析 → BuildError(file:line:col:code)
- `BuildFixLoop` + `IFixStrategy` + `RuleBasedFixStrategy`：迭代闭环（跑→解析→策略出 patch→应用→再跑），带迭代上限/超时/收敛检测（连续两轮同错误签名→停）；RuleBasedFixStrategy 只做安全修复（CS1002 缺分号、CS0246/CS0103 缺 using）
- `GitWorkflow`：Status/Diff/SuggestCommitMessage(conventional commits)/CreateBranch/ExportPatch/Add/Commit，走 permCommandRun
- `CodingLoopSession`：顶层状态机 Analyze→Plan→Execute→Review→Done，复用上述全部，输出 CodingLoopReport(ToMarkdown)
- 6 个测试文件（RunAll 模式）注册进 TestRunner；csproj 注册 6 个源文件；build.ps1/run-tests.ps1 动态 glob 自动包含
- **关键编译修复**：BuildFixLoop/GitWorkflow/CodingLoopSession 在 Agent.Coding 子命名空间引用 Agent 父命名空间的 ICommandRecorder/AgentPlanStepResult，必须加 `using ZhuaQianDesktopApp.Agent;`（C# 子命名空间不自动看见父命名空间类型）——已补
- 两套实现命名空间隔离、类名不冲突，可共存；后续由用户决定合并策略（本套有完整测试 + csproj 注册 + dry-run/回滚/前置条件/收敛检测等安全设计）
- 设计文档 `docs/CODING_LOOP_DESIGN.md`

## 浏览器渲染抓取（Epic G，本轮交付）

用户点名"下一步最该补浏览器渲染抓取"，用于读 JS 渲染 / 反爬 / 登录态网站。
原静态 `WebSearchClient.FetchPage`(WebClient) 是盲区；`WebPageReportBuilder` 已留 TODO。

- 引擎：用户选定 **Playwright for .NET**（headless Chromium），项目首个外部 NuGet 依赖。
- 新增 `Tools/BrowserRenderClient.cs`（异步封装，产出与 `WebPageFetchResult` 兼容、加 `Html` 字段；复用 `WebSearchClient.ValidatePublicHttpUrl`；storage-state 登录态持久化；`--disable-blink-features=AutomationControlled` 基础反检测；首用 `Playwright.InstallAsync()` 自动装浏览器）。
- 新增 `Agent/BrowserFetchExecutor.cs`：`IAsyncCommandExecutor`，命令 `BrowserFetch`，权限 `permNetworkUpload`；AgentPipelineFactory 注册；AgentPlanCommandMapper 加映射。
- 新增 `Tools/WebResearchFetcher.cs`：静态 `FetchPage` 优先，空/过短/失败才回退浏览器；两处研究入口（主文件 `BuildWebPageContext` + `MainForm.LocalActionRouting`）改调它，研究自动升级。
- `src/packages.config` 固定 Microsoft.Playwright 1.48.0；build.ps1 / run-tests.ps1 动态解析+引用+复制 Playwright 及传递依赖 DLL（System.Text.Json / Microsoft.Bcl.AsyncInterfaces / System.Runtime.CompilerServices.Unsafe / System.Threading.Tasks.Extensions）+ 原生驱动到 EXE 旁；缺包清晰报错。
- 测试 `tests/TestBrowserRenderClient.cs` 只验校验门禁与回退（不启动浏览器，CI 免装浏览器）。
- 详档 `docs/patches/BROWSER_RENDER_INTEGRATION.md`（安装/构建/登录态/反爬/坑位）。
- 约束：`LangVersion=7.3`，故用显式 `CloseAsync`/`DisposeAsync`(try/finally) 而非 `await using`。
- 校验：check-architecture 通过（主文件 3625 行，预算同步上调）、check-package 通过、CS0101 扫描除正则误报 `.for` 外干净。提交 `6a674a7`。
- 风险：沙箱禁 csc，未本地编译；Playwright API/传递依赖 DLL 复制的最终正确性需用户本地 `build.ps1` + 真浏览器 `BrowserFetch` 验证。

## 浏览器交互 + 桌面控制闭环（Epic H，本轮交付）

用户要求："需要它能操作浏览器，完成工作，操控鼠标和键盘。"
盘点现状：ComputerControlExecutor 已能 open/type/hotkey/key/click/wait（Win32 mouse_event + SendKeys，但**盲操作**按坐标）；BrowserRenderClient 仅 headless 抓取（只读，Playwright 引擎未用于交互）。

新增（均贴合 AgentPipeline/CommandResult/AgentCommand、C# 7.3）：
- `Tools/BrowserAgentClient.cs`：持久会话的**浏览器交互内核**——Navigate/Click/ClickText/Fill/Type/PressKey/Submit/Screenshot/DomSnapshot/Text/Title/Url；每动作 best-effort 返回 BrowserActionResult 不抛；复用 WebSearchClient 校验与 Playwright.InstallAsync。
- `Agent/BrowserControlExecutor.cs`：命令 `BrowserControl`，`IAsyncCommandExecutor`，动作经 `permNetworkUpload`（与 BrowserFetch 同门）。
- `Tools/DesktopScreenCapture.cs`：GDI `CopyFromScreen` 截屏（PNG 字节/路径），让桌面控制从盲变可观测。
- `Agent/ScreenCaptureExecutor.cs`：命令 `ScreenCapture`，`permScreenshot`。
- `Agent/TaskAgentRunner.cs`：通用的**感知→决策→行动→验证**闭环（接口 IEnvironment/ITaskPolicy + 浏览器/桌面适配器 + ScriptedTaskPolicy + DelegateTaskPolicy 接 LLM）；含 TaskRunReport。生产路径建议把 env.Actuate 改为过管线（注释已给草图）。
- `outputs/browser-task-loop/browser_task_loop.py`：**纯标准库可运行原型**，用模拟 DOM+桌面状态实跑闭环——浏览器 5 步(填user→填pass→点登录→填title→提交)成功、桌面 3 步(开记事本→打字→截屏)成功，exit0 通过。
- `AgentPipelineFactory.cs` 注册 BrowserControlExecutor + ScreenCaptureExecutor（截图目录在 configDir/browser-shots、screen-shots）。
- 六项 C# 文件经括号配平检查均 PARENS BALANCED。
- 约束：沙箱禁 csc，C# 未本地编译；Playwright API 名称已尽量保守（GetByText 用无参重载）。`ITaskPolicy` 生产实现（接 Gemini/强代码模型）未写；闭环生产化需把 Actuate 改走 PermissionGate 管线。

## 全量静态编译审计（2026-07-18 下午）

在用户跑 `build.ps1` + `run-tests.ps1` 前，对 34 个文件做了穷尽式静态审查：

- **我的 6 模块 + 6 测试**：命名空间隔离确认（Agent.Coding vs Agent），using 补全确认
- **MainForm.DiagnoseFix.cs**：15+ 方法/字段引用逐一验证（EnsurePermission 签名、ShowApprovalCard 9 参数、AgentCommand 6 参数构造、PermissionLevel.Ask、DiagnoseFixDialog 构造+3 回调、pipeline.Run 返回 CommandResult、permGate/agentPipelineFactory/pluginDir/allowAdvancedPlugins/currentTaskId/workMode 字段存在性）
- **GitWorkflowExecutor.cs**：CommandResult.ExitCode/.Success → .Status + CommandStatus.Success 修复确认
- **AgentPipelineFactory.cs**：5 个新 Executor 构造函数签名全匹配（DiagnoseFixExecutor 无参、PatchExecutor/GitWorkflowExecutor 单参、BrowserControlExecutor 双参、ScreenCaptureExecutor 双参）
- **DiagnoseFixExecutor.cs**：CodingLoop/UnifiedPatch/ChangeKind/FixDecision/ScriptedCodingFixDecider/InteractiveCodingFixDecider 全部类型+字段验证
- **BrowserControlExecutor.cs**：子代理验证 BrowserAgentClient 14 个方法签名全对齐 + C# 7.3 兼容
- **FixLoopRunner/TaskAgentRunner/CodingLoop/ProjectScanner/DiffEngine**：子代理深审，外部类型引用+方法签名+接口实现全通过
- **IAsyncCommandExecutor : ICommandExecutor** 继承链确认 → Register(ICommandExecutor) 可接受 async executor
- **项目级 C# 8+ 扫描**：零匹配（record/init/await using/string?/switch表达式）
- **build.ps1 排除确认**：Program.cs/MainForm.cs/TaskInfo.cs 三死文件排除
- **结论：零编译阻断，build.ps1 应一次通过**

### 并行进程未提交改动的发现与入库

静态审计中发现 git 工作树有 8 个文件被并行进程修改但未提交。逐一审查后提交（`fb27f1d`）：

- **`BrowserActionResult.Ok(...)` → `.Success(...)`**：关键修复！`Ok` 是字段不是方法，调 `BrowserActionResult.Ok("...")` 会 CS1955（non-invocable member used like method）。并行进程加了 `Success`/`Fail` 静态工厂方法 + 全部调用点改名。
- **`PatchExecutor.cs` ReplaceOnce**：`string.Replace(old,new,1)` 3 参数重载在 .NET Framework 4.8 不存在（是 .NET Core 2.1+），改为自定义 `ReplaceOnce`（IndexOf + Substring）。
- **`TaskAgentRunner.cs` + `BrowserAgentClient.cs` 空 catch 修复**：`catch { }` → `catch (Exception ex) { Debug.WriteLine(...); }`，消除 run-tests.ps1 的空 catch 扫描误报。
- **`run-tests.ps1` packages 路径修复**：`src\packages`（实际变成 `src/src/packages`）→ `packages`（正确指向 `src/packages`）。
- **`DiagnoseFixExecutor.cs` 大重构**：从 `CodingLoop`（Agent 命名空间）切到我的 `CodingLoopSession` + `BuildFixLoopOptions` + `RuleBasedFixStrategy`（Agent.Coding 命名空间）——**并行进程采纳了我的实现**。加 `using ZhuaQianDesktopApp.Agent.Coding;` + `using ZhuaQianDesktopApp.Core;`。
- **`MainForm.DiagnoseFix.cs` 增强**：加 `ChooseDiagnoseFixProject()`（文件夹选择对话框入口）、`ExecuteDiagnoseFixForRootAsync()`（异步执行核心）、`ShowDiagnoseFixDialog()`（提取对话框展示）。4 参数 `DiagnoseFixDialog(markdown, root, Tr, uiLanguage)` 构造函数。
- **`ZhuaQianDesktop.cs`**：工具面板 + 命令面板加 "Diagnose & Fix Project" 入口，调 `ChooseDiagnoseFixProject()`。
- **行预算同步**：`docs/_line_budget.json` maxMainLines 3625→3630（实际 3627 行，并行进程净增 0 行——加 2 行删 2 空行）。
- **架构检查 + 包检查**：均在沙箱实跑通过。
- **.gitignore**：加 `src/packages/`（NuGet restore 目录，不应入库）。
