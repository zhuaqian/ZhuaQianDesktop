# 2026-07-18 工作记录

## 深度架构审计（ZhuaQianDesktop / ZhuaQian 项目）

- 跑通 `src/scripts/check-architecture.ps1`：架构检查 **PASS**。主文件 `ZhuaQianDesktop.cs` = 3624 行 = `maxMainLines` 上限（零余量）；无非主 .cs 超 900 行（最大 `RightPanel.cs` 593）。
  - 教训：本环境 `wc -l` 对 CRLF 文件高估 ~271 行（误报主文件 3895），预算敏感计数须用 PowerShell `Measure-Object -Line`（即 check-architecture 所用口径）。
- 孤儿/引用审计发现 `PluginRegistry.cs` 被 0 个生产文件引用，且引用未定义类型 `PluginSandboxSettings` → 编译 CS0246。
- **根因**：`build.ps1`/`run-tests.ps1` 动态枚举重写（commit a123ec5）后，排除列表仅 `Program.cs/MainForm.cs/TaskInfo.cs`，不再小心"漏掉"坏孤儿 `PluginRegistry.cs`，导致两脚本当前**实际已坏**（backlog "190/0 通过"已过时）。
- **处置**：退役 `src/Plugins/PluginRegistry.cs`（`rm`，git 跟踪可还原）+ 移除 csproj 第 61 行 Compile Include。项目单一插件方案收敛为 `PluginManifest`（数据契约，已测）+ `HookRegistry`（观察缝，已接 AgentPipeline）。
- 已同步更新 `docs/_collab_board.md`、`docs/EXECUTION_BACKLOG.md`（Epic E）、`docs/patches/EPIC_E_INTEGRATION.md`（标注退役 + build 动态）。
- 未编译验证（沙箱禁 csc）。建议用户本地 `build.ps1`+`run-tests.ps1` 复跑确认全绿。

## 当前 Epics 状态（2026-07-18）
- A/B/C/D/E/F 代码模块基本落地；F2（写高价值 office 文件前审查步骤）、F3（生成前用 web 研究）、F4（示例）仍为明确 TODO。
- 待办：本地构建复跑；可选继续 Epic F2/F3/F4 或接 UI 入口让 OfficeTemplateLibrary 真正被调用（当前为纯库、无调用方）。

## 文档整理（用户指令「整理文档，全部做完再开源」）
- 把 docs/ 根目录 19 篇早期协调/进度/评估碎片 `git mv` 归档至 `docs/archive/coordination-2026-07/`（BUG_REVIEW、CODE_COMPLETION_ALIGNMENT、COORDINATION、CURRENT_GAPS/REALITY、DOCUMENTATION_MAP、EOE_SCENARIOS、GRANULARITY_REVIEW×2、OPEN_SOURCE_READINESS_REVIEW、PERMISSION_UPGRADE_SCHEMA、PROMOTION_PROSPECTS、PROGRESS_*×5、VERIFICATION、SLN_MIGRATION_PLAN）。可逆，遵循项目既有 archive 约定。
- 过时补丁 `EPIC_D_INTEGRATION.md` / `NEW_MODULES_BUILD_REGISTRATION.md` 归档至 `docs/archive/patches-obsolete/`；保留 `docs/patches/EPIC_E_INTEGRATION.md`（并行进程在制品，勿碰）。
- 新增 `docs/INDEX.md` 作为文档导航中枢（规范/插件/MCP/开源/交接/实时协作/归档 分类）。
- 刷新 README.md：删已退役 `work/zq-desktop` 测试脚本引用；Current Status 测试数改为「末次干净验证 186，新增 installer+F1+退役 PluginRegistry 后需本地重跑确认（预期 ~189–190）」；Agent 工作流改为已有持久状态机；Not implemented 细化（MCP 仅调研、签名插件仅指引未强制、缺标准测试框架、企业安全/内核监控未做）；Read First 去掉已归档项并加 INDEX.md；补「开源：全部做完才发布」段。
- 修 3 篇保留文档的断链：PROJECT_HANDOFF（信任清单+阅读顺序）、PRE_OPENSOURCE_CHECKLIST（保留表改为当前集+标注 2026-07-18 归档）、_parallel_coordination_prompts（并行进程读哪些文档）。
- 未碰 in-flight 文件（_collab_board.md、EXECUTION_BACKLOG.md、patches/EPIC_E_INTEGRATION.md）；未碰 `outputs/`（生成快照，README 规定非源）。
- 根目录现状：14 篇规范 .md + 3 个 `_` 实时协作文件 + _line_budget.json/_last_verification.txt + archive/ + patches/。

## Epic F2/F3 办公生成闭环收口（续前「继续工作」）

把 F1 纯库 `OfficeTemplateLibrary` 接到真实入口，闭环 Epic F。全部净新增/接线，未碰 Process X 独占文件。

- 新增 `src/Agent/OfficeTemplateExecutor.cs`（110 行，ICommandExecutor，CommandType="OfficeTemplate"）：优先用调用方传入、用户审查后的最终文本；缺文本时按 kind+结构化字段 `OfficeTemplateLibrary.Render` 兜底；经 `OfficeExporter` 导出 7 种格式；ParseKind 接受枚举名/RenderByName/回退 Report。
- 新增 `src/ui/OfficeGenerateDialog.cs`（276 行，Form）：模板下拉(5类)+主题/标题/副标题/作者/日期+要点/表行+实时预览(Consolas)+「Use Web Research」(F3→WebSearchClient.SearchDetailed)+「Refresh Preview」+「Copy」+「Generate」(显式 SaveFileDialog=F2 写前审查)。镜像 PlanReviewDialog 的 T(en,zhHans,zhHant)/IsEnglish 约定。
- `src/Agent/AgentPipelineFactory.cs`：`RegisterStandardExecutors` 加一行 `pipeline.Register(new OfficeTemplateExecutor(officeExporter));`（工厂单点登记，与 Epic E 一致）。
- `src/ui/MainForm.LocalActionRouting.cs`（partial，不增主文件行数）：`TryRunNaturalLocalAction` 新增 `LooksLikeOfficeGenerateRequest(lower)`→`ExecuteOfficeGenerate(text)` 分支；`ExecuteOfficeGenerate` 复制 `RunComputerControl` 的审批流（EnsurePermission(permFileWrite) + PermissionGate.FromJson + Set("permFileWrite",Ask) + ShowApprovalCard + pipeline.Run(AgentCommand("OfficeTemplate",...))）。复用既有 Tr/EnsurePermission/ShowApprovalCard/LogAction/RecordAction/SetCurrentTaskStatus。
- 新增 `src/tests/TestOfficeTemplateExecutor.cs`（RunAll：工厂登记 + 带文本导出 + 缺文本兜底 + 五类 kind 端到端）；`src/tests/TestRunner.cs` 接 `failures += TestOfficeTemplateExecutor.RunAll();` 并补 `HasExecutor("OfficeTemplate")` 断言。预期 190→191 tests。
- `src/ZhuaQianDesktop.csproj`：补 `Agent\OfficeTemplateExecutor.cs`(行78)+`ui\OfficeGenerateDialog.cs`(行120) Compile Include（生产 msbuild 覆盖）。

验证（沙箱禁 csc）：`check-architecture.ps1` 复跑 **PASS**（预算未破、零 new Tools. 违规、新文件 110/276 < 900）；grep 确认 OfficeTemplateExecutor/OfficeGenerateDialog/两 NL 路由符号引用自洽无悬空；build.ps1 动态枚举自动拾取、run-tests 不进生产 EXE（无 CS0017）。
**结论/待办**：所有在制品未提交、沙箱禁 csc 未经真编译。请用户本地 `build.ps1`+`run-tests.ps1` 复跑确认全绿（预期 191/0）——上次退役 PluginRegistry 刚修了一处真坏构建，本次接线须一并验证。F4 示例画廊仍 TODO（非阻塞）。
边界提醒：改了 MainForm.LocalActionRouting.cs（main-form 家族 partial），已认领；提醒 Process X 勿在 TryRunNaturalLocalAction/ExecuteOfficeGenerate 区域重复改。

## 「安排」下一波任务（用户指令「安排吧」）

进程仍在跑（git 在制品 README/EXECUTION_BACKLOG/_collab_board/_parallel_coordination_prompts/EPIC_E_INTEGRATION 均 `M`；新文件 OfficeTemplateExecutor/OfficeGenerateDialog/TestOfficeTemplateExecutor 已出）。用户批准把收尾批次排进 docs/ 让进程停手时自取。

- 新增 `docs/_next_batch_prompts.md`（净新文件，沿用 `_` 实时协作命名）：可粘贴的收尾批次提示词，按 P-A/P-B/P-C 角色分配，全部标注「当前 Epic E/F 集成落定后认领」。
- **范围锁定（「全部做完」边界）**：依 backlog "Not Recommended"——❌ 企业安全/内核监控/反作弊驱动、❌ 真实 MCP client（仅调研）；✅ Installer 已交付不再列待办。**开源锁死为最后一步**，仅用户给仓库地址+凭证后执行。
- **收尾真实剩余** = ① F4 示例画廊（OfficeGenerateDialog 5 套预设，grep 确认 preset/gallery/example 全空=真缺口）② 开源仓库卫生（.gitignore 收紧+全仓/历史密钥扫描+遗留脚本清理+四件套一致性）③ 最终验证闸门（build+test 全绿，预期 ~191）④ 开源（闸门）。
- **卫生发现待处置**：`src/zq_desktop.ps1`（574 行旧单文件脚本，含 apiKey 输入框）仍为 tracked 遗留 PoC，开源前需 git mv 归档或明确保留；根 `display_showsettings.ps1`/`extract_method.ps1`/`final_analysis.ps1` 疑开发杂项脚本，应移 tools/ 或 archive。已确认：`work/` 目录已清、无 .env/密钥入库、LICENSE/CONTRIBUTING/SECURITY/README 四件套齐、.gitignore 已覆盖 dist/bin/obj/outputs/.env/secret*.json/*.exe/*.pdb。
- 未碰任何 in-flight 文件；不编辑 _collab_board/EXECUTION_BACKLOG（避免与进程抢写）。

## 哨兵自动化：进程停手自动派发收尾批次（用户指令「他们停了，你叫他们继续」）

用户睡后委托我监控并行进程并在其停手时自动派发收尾任务。

- 01:00 现状：进程**仍运行**——git 在制品（README/_collab_board/EXECUTION_BACKLOG/_parallel_coordination_prompts/EPIC_E_INTEGRATION 全 `M`）+ F 集成三新文件 `??` 未落地，最后提交停在 00:08。未停手。
- 局限说明：本会话无法给另一个 WorkBuddy 会话发实时消息；并行进程只能借共享文件协调（见 `_parallel_coordination_prompts.md`）。故「叫他们继续」= 停手后把收尾任务排进它们会读的 `_collab_board.md`。
- 已建自动化 `automation-1784307802347`（ZhuaQian 收尾派发哨兵，ACTIVE，HOURLY）：每小时 `git status --short` 探测；仅当 (a) 5 个协调/集成文件无 `M` 且 (b) F 三新文件不再 `??` 时判定停手；防重（看板已有 Finishing 段则跳过）；仅往 `_collab_board.md` 末尾追加「收尾批次（Finishing）」4 项 todo（F4/开源卫生/验证闸门/开源闸门）。安全铁律：不 commit、不改 src/、不跑 build/test、只追加看板。
- 诚实边界：若那几个进程不是自主轮询型（需用户粘贴才动），哨兵的派发会在看板就位，待用户粘贴 `_next_batch_prompts.md` 或进程下次读看板时生效。

## 用户指令「停止监控」（11:03）

- 删除了哨兵自动化 `automation-1784307802347`（HOURLY）。删除前从未触发：看板无 Finishing 段；进程整夜未停，11:03 时 `M` 文件比昨晚更多（新增 src/Tools/WebPageReportBuilder.cs、docs/RELEASE_TRUST_PIPELINE.md、scripts/export-review-snapshot.ps1 等未跟踪项），哨兵一直判为「仍在跑」noop。
- 收尾派发交还手动：`docs/_next_batch_prompts.md` 保留就绪，待用户后续粘贴给进程或进程读看板时生效。
